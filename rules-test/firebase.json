{
  "_comment": "No firestore.rules path here on purpose: the CLI rejects a rules file outside its project directory, and test-rules.mjs loads ../firestore.rules itself and uploads it to the emulator. Adding a rules entry here would only create a second place the ruleset could drift.",
  "emulators": {
    "firestore": { "port": 8080, "host": "127.0.0.1" },
    "ui": { "enabled": false }
  }
}
